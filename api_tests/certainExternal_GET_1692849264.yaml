# Test generated by RoostGPT for test PostmanTest using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/1234/abcd"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"
            - hasProperty: "uuid"
            - hasProperty: "dateCreated"
            - hasProperty: "dateModified"
            - hasProperty: "externalKey"
            - hasProperty: "isActive"
            - hasProperty: "prefix"
            - hasProperty: "suffix"
            - hasProperty: "firstName"
            - hasProperty: "middleName"
            - hasProperty: "lastName"
            - hasProperty: "badgeName"
            - hasProperty: "dob"
            - hasProperty: "position"
            - hasProperty: "organization"
            - hasProperty: "phone"
            - hasProperty: "phone2"
            - hasProperty: "phoneMobile"
            - hasProperty: "fax"
            - hasProperty: "email"
            - hasProperty: "ccAssistantOnEmails"
            - hasProperty: "ccAssociateOnEmails"
            - hasProperty: "accountCode"
            - hasProperty: "profilePin"
            - hasProperty: "pkProfileId"
            - hasProperty: "showOnline"
            - hasProperty: "imported"
            - hasProperty: "source"
            - hasProperty: "media"
            - hasProperty: "socialMedias"
            - hasProperty: "privatePersonal"
            - hasProperty: "assistant"
            - hasProperty: "travel"
            - hasProperty: "associate"
            - hasProperty: "notes"
            - hasProperty: "address"
            - hasProperty: "altAddress"
            - hasProperty: "shipAddress"
            - hasProperty: "profileQuestions"
            - hasProperty: "password"
            - hasProperty: "events"
            - hasProperty: "dataPrivacyPolicies"
            - hasProperty: "integrationExternalKeys"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid/url"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/12345678901234567890/abcd"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"
            - hasProperty: "uuid"
            - hasProperty: "dateCreated"
            - hasProperty: "dateModified"
            - hasProperty: "externalKey"
            - hasProperty: "isActive"
            - hasProperty: "prefix"
            - hasProperty: "suffix"
            - hasProperty: "firstName"
            - hasProperty: "middleName"
            - hasProperty: "lastName"
            - hasProperty: "badgeName"
            - hasProperty: "dob"
            - hasProperty: "position"
            - hasProperty: "organization"
            - hasProperty: "phone"
            - hasProperty: "phone2"
            - hasProperty: "phoneMobile"
            - hasProperty: "fax"
            - hasProperty: "email"
            - hasProperty: "ccAssistantOnEmails"
            - hasProperty: "ccAssociateOnEmails"
            - hasProperty: "accountCode"
            - hasProperty: "profilePin"
            - hasProperty: "pkProfileId"
            - hasProperty: "showOnline"
            - hasProperty: "imported"
            - hasProperty: "source"
            - hasProperty: "media"
            - hasProperty: "socialMedias"
            - hasProperty: "privatePersonal"
            - hasProperty: "assistant"
            - hasProperty: "travel"
            - hasProperty: "associate"
            - hasProperty: "notes"
            - hasProperty: "address"
            - hasProperty: "altAddress"
            - hasProperty: "shipAddress"
            - hasProperty: "profileQuestions"
            - hasProperty: "password"
            - hasProperty: "events"
            - hasProperty: "dataPrivacyPolicies"
            - hasProperty: "integrationExternalKeys"
