# Test generated by RoostGPT for test PostmanTest using AI Type Open AI and AI Model gpt-4

config:
  target: 'https://example.com'
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: 'Successful API request'
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ $randomNumber(1000,9999) }}/{{ $randomNumber(1000,9999) }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: 'profileId'
            - hasProperty: 'uuid'
            - hasProperty: 'dateCreated'
            - hasProperty: 'isActive'
            - hasProperty: 'firstName'
            - hasProperty: 'lastName'
            - hasProperty: 'accountCode'
            - hasProperty: 'profilePin'
  - name: 'API request with validation failure'
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/invalid/invalid"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
  - name: 'API request with edge case'
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ $randomString(1000) }}/{{ $randomString(1000) }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: '{{ $randomNumber(200,500) }}'
